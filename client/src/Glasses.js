class Glasses {
  constructor(data) {
    console.log(data);
    // const name2 = data;
    this.wine = data;

    // const slacked = this.name;
    // console.log(slacked);

    // const yokel = slacked.map(result => console.log(result));
    // const cletus = Array.from(this.name);
    // console.log(cletus);

    // this.winenum = data.wines.winenum;

    // this.funfact = data.wines.funfact;
    // console.log(this.funfact);
  }
}

export default Glasses;
